server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/AdServer
    username: root
    password: 123456789
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: validate
  redis:
    host: localhost
    database: 2
    port: 6379
    jedis:
      pool:
        max-active: 8
